{
  "name": "habitos-monorepo",
  "workspaces": [
    "frontend",
    "backend"
  ],
  "scripts": {
    "setup": "chmod +x setup.sh && ./setup.sh",
    "dev:back": "bun run --cwd backend dev",
    "dev:front": "bun run --cwd frontend start",
    "dev": "bun run dev:back & bun run dev:front"
  }
}
